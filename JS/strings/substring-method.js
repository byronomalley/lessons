var data = "name|phone|address";// the main string
var val = data.substring(5, 10);// we want the string from index 5 up to, but not including 10 returned -- the second arguement can be ommitted and the whole string starting from index 5 will be logged
console.log("Substing is " + val);
